import { T } from "../libs/types/common";

// This controller handles routes for regular users (React frontend)
// It will grow as you add features like login, signup, profile, etc.

const memberController: T = {}

// routes will be added here as the React app is built
// example:
// memberController.goHome = (req: Request, res: Response) => {
//     res.sendFile(...)  // serve the React index.html
// }

export default memberController;
